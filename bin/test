#!/usr/bin/env bash
USAGE="test <command>: test the opencode installation path for asdf"

case "${1:-}" in
"-h" | "--help" | "help")
    printf "%s\n" "${USAGE}"
    exit 0
    ;;
*)
    exit 0
esac



