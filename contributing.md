# Contributing

Testing Locally:

```shell
asdf plugin test plugin-opencode https://github.com/jikanter/asdf-plugin-opencode.git [--asdf-tool-version <version>] [--asdf-plugin-gitref <git-ref>] bin/test


```

Tests are automatically run in GitHub Actions on push and PR.
